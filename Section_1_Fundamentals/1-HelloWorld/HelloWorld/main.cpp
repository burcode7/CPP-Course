#include <iostream> // Includes the "iostream" library, which allows us to interact with the user, input & out, to add to the console

using namespace std; // Sets "std" or "standard" as the default namespace, allowing us to ignore "std::"

int main()
{
	cout << "Hello Ben" << endl; // Prints any text and ends the line. "cout" stands for "console output"

	return 0; // Signals to the system that nothing went wrong
}